<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet & Network State (for API calls + connectivity_plus) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <!-- Microphone & Audio Recording (record + speech_to_text) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>

    <!-- Storage for sharing exported CSV (Android 12 and below) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- ميزة الميكروفون: غير إلزامية (required="false") لأن التطبيق يوفر
         مساراً بديلاً كاملاً وفعّالاً لإدخال الدرجات يدوياً عبر لوحة
         المفاتيح (انظر GradeFieldCard.TextField مع keyboardType رقمي) —
         التسجيل الصوتي ميزة مساعدة اختيارية فقط. لو تُرك required="true"
         (كما كان سابقاً)، فسيمنع متجر Google Play تثبيت التطبيق تلقائياً
         على أي جهاز بدون ميكروفون فعلي (بعض الأجهزة اللوحية/التلفزيونات
         الذكية)، رغم أن التطبيق يعمل بكامل وظائفه بدونه. VoiceService
         يتعامل بالفعل بأمان مع حالة رفض الصلاحية (denied/permanentlyDenied)
         ويسمح للمستخدم بالمتابعة يدوياً، لذا لا حاجة لفرض الميزة على مستوى
         النظام. -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:label="Study Grades Voice"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for speech_to_text on Android 11+ -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>


